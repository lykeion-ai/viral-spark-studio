import { EditorStage } from '@/components/EditorStage';

const Edit = () => {
  return (
    <div className="min-h-screen bg-background w-full">
      <EditorStage />
    </div>
  );
};

export default Edit;
